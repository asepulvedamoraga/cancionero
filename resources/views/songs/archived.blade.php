@extends('layouts.app')
@section('title','Canciones archivadas')
@section('content')
<div class="app-page-header"><div><h1>Canciones archivadas</h1><p>Restaura canciones sin perder sus archivos ni relaciones.</p></div><a class="btn btn-outline-secondary" href="{{ route('songs.index',['scope'=>'mine']) }}"><i class="bi bi-arrow-left"></i>Mis canciones</a></div>
<form class="card card-body app-filter-card" method="GET"><div class="row g-2"><div class="col-md-9"><label class="visually-hidden" for="archived-q">Buscar canción archivada</label><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" id="archived-q" name="q" value="{{ request('q') }}" placeholder="Título o autor"></div></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-primary flex-grow-1">Buscar</button><a class="btn btn-outline-secondary" href="{{ route('songs.archived') }}">Limpiar</a></div></div></form>
<div class="card"><div class="list-group list-group-flush">@forelse($songs as $song)<div class="list-group-item d-flex flex-wrap justify-content-between align-items-center gap-3 py-3"><div><strong>{{ $song->title }}</strong><div class="small text-secondary">{{ $song->author ?: 'Autor no indicado' }} · Subida por {{ $song->owner->name }}</div><div class="small text-secondary">Archivada el {{ $song->deleted_at->format('d-m-Y H:i') }}</div></div><form method="POST" action="{{ route('songs.restore',$song->id) }}">@csrf @method('PUT')<button class="btn btn-outline-primary"><i class="bi bi-arrow-counterclockwise"></i>Restaurar</button></form></div>@empty<div class="app-empty-state"><i class="bi bi-archive"></i><h2 class="h4">No hay canciones archivadas</h2><p class="mb-0">Las canciones que archives aparecerán aquí.</p></div>@endforelse</div></div><div class="mt-4">{{ $songs->links() }}</div>
@endsection