@extends('layouts.app')
@section('title', 'Papelera de repertorios')
@section('content')
<div class="app-page-header"><div><h1>Papelera de repertorios</h1><p>Restaura repertorios conservando sus canciones, orden y notas. Por seguridad se restauran como privados.</p></div><a class="btn btn-outline-secondary" href="{{ route('repertoires.index') }}"><i class="bi bi-arrow-left"></i>Repertorios</a></div>
<nav class="app-tabs mb-4" aria-label="Secciones de repertorios"><a class="app-tab" href="{{ route('repertoires.index') }}"><i class="bi bi-list-ol"></i>Repertorios</a><a class="app-tab active" href="{{ route('repertoires.trashed') }}" aria-current="page"><i class="bi bi-trash3"></i>Papelera</a></nav>
<form class="card card-body app-filter-card" method="GET" role="search"><div class="row g-2"><div class="col-md-9"><label class="visually-hidden" for="trashed-repertoire-q">Buscar repertorio eliminado</label><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" id="trashed-repertoire-q" name="q" value="{{ request('q') }}" placeholder="Nombre o lugar"></div></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-primary flex-grow-1"><i class="bi bi-search"></i>Buscar</button><a class="btn btn-outline-secondary" href="{{ route('repertoires.trashed') }}">Limpiar</a></div></div></form>
<div class="card"><div class="list-group list-group-flush">@forelse($repertoires as $repertoire)<div class="list-group-item d-flex flex-wrap justify-content-between align-items-center gap-3 py-3"><div><strong>{{ $repertoire->name }}</strong><div class="small text-secondary">{{ $repertoire->event_date?->format('d-m-Y') ?: 'Sin fecha' }} · {{ $repertoire->songs_count }} {{ $repertoire->songs_count === 1 ? 'canción' : 'canciones' }}</div>@if(auth()->user()->is_admin)<div class="small text-secondary">Creado por {{ $repertoire->owner->name }}</div>@endif<div class="small text-secondary">Enviado a la papelera el {{ $repertoire->deleted_at->format('d-m-Y H:i') }}</div></div><form method="POST" action="{{ route('repertoires.restore', $repertoire->id) }}">@csrf @method('PUT')<button class="btn btn-outline-primary"><i class="bi bi-arrow-counterclockwise"></i>Restaurar</button></form></div>@empty<div class="app-empty-state"><i class="bi bi-trash3"></i><h2 class="h4">La papelera está vacía</h2><p class="mb-0">Los repertorios eliminados aparecerán aquí.</p></div>@endforelse</div></div><div class="mt-4">{{ $repertoires->links() }}</div>
@endsection