@extends('layouts.app')
@section('title','Editar '.$song->title)
@section('content')<div class="app-page-header"><div><h1 class="h2">Editar canción</h1><p class="text-secondary mb-0">{{ $song->title }}</p></div><a class="btn btn-outline-primary" href="{{ route('songs.show',$song) }}"><i class="bi bi-eye"></i>Ver canción</a></div><form class="card card-body mb-4 song-upload-form" method="POST" action="{{ route('songs.update',$song) }}" enctype="multipart/form-data" data-song-upload-form data-song-upload-message="Estamos convirtiendo el PDF y generando sus páginas. No cierres esta ventana hasta que termine.">@csrf @method('PUT') @include('songs._form')</form>
@if($song->files->isNotEmpty())<div class="card"><div class="card-header app-section-header"><strong>Páginas y documentos</strong><span class="text-secondary ms-2">Arrastra para ordenar</span></div><div class="card-body"><div class="song-file-grid" data-song-files data-reorder-url="{{ route('songs.files.reorder',$song) }}">@foreach($song->files as $file)<article class="song-file-card" data-file-id="{{ $file->id }}"><div class="drag-handle" title="Arrastrar">⋮⋮</div>@if($file->file_type==='pdf')<div class="pdf-placeholder"><i class="bi bi-file-earmark-pdf"></i><span>PDF</span></div>@else<img src="{{ route('songs.files.preview',[$song,$file]) }}" alt="Vista previa de {{ $file->original_name }}">@endif<div class="p-2"><small class="d-block text-truncate">{{ $file->original_name }}</small><div class="btn-group btn-group-sm mt-2"><a class="btn btn-outline-primary" href="{{ route('songs.files.show',[$song,$file]) }}" target="_blank">Abrir</a><a class="btn btn-outline-secondary" href="{{ route('songs.files.download',[$song,$file]) }}">Descargar</a></div><form class="mt-2 song-upload-form" method="POST" action="{{ route('songs.files.replace',[$song,$file]) }}" enctype="multipart/form-data" data-song-upload-form data-song-upload-message="Estamos reemplazando el archivo y convirtiendo el PDF. Espera a que termine el proceso.">@csrf @method('PUT')<label class="btn btn-sm btn-outline-secondary w-100">Reemplazar<input class="visually-hidden" type="file" name="file" accept="image/jpeg,image/png,image/webp,application/pdf" required onchange="this.form.submit()"></label></form><form class="mt-2" method="POST" action="{{ route('songs.files.destroy',[$song,$file]) }}" data-confirm="¿Eliminar este archivo?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger w-100">Eliminar</button></form></div></article>@endforeach</div><div class="small mt-3" data-sort-status></div></div></div>@endif
@endsection
