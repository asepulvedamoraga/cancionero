@extends('layouts.app')
@section('title',$song->title)
@section('content')
<div class="app-page-header align-items-start"><div><h1 class="h2 mb-1">{{ $song->title }}</h1><p class="text-secondary mb-0">{{ $song->author ?: 'Autor no indicado' }}</p></div><div class="app-page-actions"><a class="btn btn-dark" href="{{ route('songs.read',$song) }}"><i class="bi bi-book"></i>Modo lectura</a>@can('update', $song)<a class="btn btn-primary" href="{{ route('songs.edit',$song) }}"><i class="bi bi-pencil"></i>Editar</a>@endcan<a class="btn btn-outline-secondary" href="{{ route('songs.index') }}"><i class="bi bi-arrow-left"></i>Volver</a></div></div>
<div class="row g-4 mb-4"><div class="col-lg-8"><div class="card card-body"><dl class="row mb-0"><dt class="col-sm-4">Intérprete</dt><dd class="col-sm-8">{{ $song->performer ?: '—' }}</dd><dt class="col-sm-4">Tonalidad</dt><dd class="col-sm-8">{{ $song->musical_key ?: '—' }}</dd><dt class="col-sm-4">Categoría</dt><dd class="col-sm-8">{{ $song->category?->name ?: '—' }}</dd><dt class="col-sm-4">Momento litúrgico</dt><dd class="col-sm-8">{{ $song->liturgicalMoment?->name ?: '—' }}</dd><dt class="col-sm-4">Tiempos</dt><dd class="col-sm-8">{{ $song->liturgicalSeasons->pluck('name')->join(', ') ?: '—' }}</dd><dt class="col-sm-4">Fuente</dt><dd class="col-sm-8">{{ $song->source ?: '—' }}</dd><dt class="col-sm-4">Observaciones</dt><dd class="col-sm-8" style="white-space: pre-line;">{{ $song->notes ?: '—' }}</dd></dl></div></div><div class="col-lg-4"><div class="card card-body"><strong>Estado</strong><span class="mt-2 badge {{ $song->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $song->is_active ? 'Activa' : 'Inactiva' }}</span><div class="small text-secondary mt-3"><i class="bi bi-person"></i> Subida por {{ $song->owner->name }}</div><hr><p class="small text-secondary">Puedes incorporar esta canción al crear o editar un repertorio.</p><a class="btn btn-outline-primary" href="{{ route('repertoires.index') }}"><i class="bi bi-list-ol"></i>Ir a repertorios</a></div></div></div>
<h2 class="h4 mb-3">Páginas y documentos</h2><div class="song-pages">@forelse($song->files as $file)<article class="card song-page"><div class="card-body">@if($file->file_type==='pdf')<div class="pdf-placeholder large"><i class="bi bi-file-earmark-pdf"></i><span>{{ $file->original_name }}</span></div>@else<a href="{{ route('songs.files.show',[$song,$file]) }}" target="_blank"><img src="{{ route('songs.files.show',[$song,$file]) }}" alt="{{ $file->original_name }}"></a>@endif<div class="d-flex flex-wrap gap-2 mt-3"><a class="btn btn-sm btn-outline-primary" target="_blank" href="{{ route('songs.files.show',[$song,$file]) }}">Abrir</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('songs.files.download',[$song,$file]) }}">Descargar original</a></div></div></article>@empty<div class="card card-body text-secondary">Esta canción todavía no tiene páginas.</div>@endforelse</div>
@can('delete', $song)<form class="app-danger-zone" method="POST" action="{{ route('songs.destroy',$song) }}" data-confirm="¿Archivar esta canción? Los archivos se conservarán.">@csrf @method('DELETE')<button class="btn btn-outline-danger"><i class="bi bi-archive"></i>Archivar canción</button></form>@endcan
@endsection
