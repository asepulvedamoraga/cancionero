@extends('layouts.app')
@section('title','Canciones')
@section('content')
<div class="app-page-header"><div><h1>Canciones</h1><p>{{ $scope === 'mine' ? 'Administra las canciones que has subido.' : 'Explora las canciones activas compartidas por la comunidad.' }}</p></div><div class="app-page-actions"><a class="btn btn-primary" href="{{ route('songs.create') }}"><i class="bi bi-plus-lg"></i>Nueva canción</a></div></div>
<nav class="app-tabs mb-4" aria-label="Secciones de canciones"><a class="app-tab {{ $scope === 'library' ? 'active' : '' }}" href="{{ route('songs.index') }}"><i class="bi bi-collection"></i>Biblioteca</a><a class="app-tab {{ $scope === 'mine' ? 'active' : '' }}" href="{{ route('songs.index',['scope'=>'mine']) }}"><i class="bi bi-person-music"></i>Mis canciones <span class="badge text-bg-light">{{ $ownCount }}</span></a><a class="app-tab" href="{{ route('songs.archived') }}"><i class="bi bi-archive"></i>Archivadas @if($archivedCount)<span class="badge text-bg-light">{{ $archivedCount }}</span>@endif</a></nav>
<form class="card card-body app-filter-card" method="GET"><input type="hidden" name="scope" value="{{ $scope }}"><div class="row g-2"><div class="col-lg-4"><label class="visually-hidden" for="song-q">Buscar por título o autor</label><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input id="song-q" class="form-control" name="q" value="{{ request('q') }}" placeholder="Título o autor"></div></div>@if($scope === 'library')<div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-ownership">Procedencia</label><select id="song-ownership" class="form-select" name="ownership"><option value="">Todas las personas</option><option value="mine" @selected(request('ownership')==='mine')>Subidas por mí</option><option value="others" @selected(request('ownership')==='others')>Subidas por otros</option></select></div>@endif<div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-category">Categoría</label><select id="song-category" class="form-select" name="category_id"><option value="">Categoría</option>@foreach($categories as $item)<option value="{{ $item->id }}" @selected(request('category_id')==$item->id)>{{ $item->name }}</option>@endforeach</select></div><div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-moment">Momento litúrgico</label><select id="song-moment" class="form-select" name="liturgical_moment_id"><option value="">Momento</option>@foreach($moments as $item)<option value="{{ $item->id }}" @selected(request('liturgical_moment_id')==$item->id)>{{ $item->name }}</option>@endforeach</select></div><div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-season">Tiempo litúrgico</label><select id="song-season" class="form-select" name="liturgical_season_id"><option value="">Tiempo</option>@foreach($seasons as $item)<option value="{{ $item->id }}" @selected(request('liturgical_season_id')==$item->id)>{{ $item->name }}</option>@endforeach</select></div>@if($scope === 'mine')<div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-status">Estado</label><select id="song-status" class="form-select" name="status"><option value="">Cualquier estado</option><option value="active" @selected(request('status')==='active')>Activa</option><option value="inactive" @selected(request('status')==='inactive')>Inactiva</option></select></div>@endif<div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-per-page">Resultados por página</label><select id="song-per-page" class="form-select" name="per_page" onchange="this.form.submit()"><option value="12" @selected($songs->perPage() === 12)>12 por página</option><option value="24" @selected($songs->perPage() === 24)>24 por página</option><option value="48" @selected($songs->perPage() === 48)>48 por página</option></select></div><div class="col-sm-6 col-lg-2"><label class="visually-hidden" for="song-sort">Ordenar por</label><select id="song-sort" class="form-select" name="sort"><option value="title">Título A-Z</option><option value="title_desc" @selected(request('sort')==='title_desc')>Título Z-A</option><option value="newest" @selected(request('sort')==='newest')>Más recientes</option><option value="oldest" @selected(request('sort')==='oldest')>Más antiguas</option></select></div><div class="col-sm-6 col-lg-2 d-flex gap-2"><button class="btn btn-primary flex-grow-1"><i class="bi bi-funnel"></i>Filtrar</button><a class="btn btn-outline-secondary" href="{{ route('songs.index',$scope === 'mine' ? ['scope'=>'mine'] : []) }}"><i class="bi bi-x-circle"></i>Limpiar</a></div></div></form>
<div class="app-list-toolbar"><p class="app-result-count">@if($songs->total()) Mostrando <strong>{{ $songs->firstItem() }}–{{ $songs->lastItem() }}</strong> de <strong>{{ $songs->total() }}</strong> canciones @else Sin resultados @endif</p>@if($songs->hasPages())<div class="app-list-pagination">{{ $songs->onEachSide(1)->links() }}</div>@endif</div><div class="row g-3">@forelse($songs as $song)<div class="col-md-6 col-xl-4"><article class="card song-card h-100">@if($song->files->first())<img class="song-cover" src="{{ route('songs.files.preview',[$song,$song->files->first()]) }}" alt="Primera página de {{ $song->title }}">@else<div class="song-cover song-cover-empty"><i class="bi bi-music-note-beamed"></i></div>@endif<div class="card-body"><div class="d-flex justify-content-between gap-2"><h2 class="h5">{{ $song->title }}</h2><span class="badge {{ $song->user_id === auth()->id() ? 'text-bg-primary' : 'text-bg-light' }}">{{ $song->user_id === auth()->id() ? 'Mía' : 'Compartida' }}</span></div><p class="text-secondary mb-2">{{ $song->author ?: 'Autor no indicado' }}</p><div class="small text-secondary mb-2"><i class="bi bi-person"></i> Subida por {{ $song->owner->name }} @if(!$song->is_active)<span class="badge text-bg-secondary ms-1">Inactiva</span>@endif</div><div class="small text-secondary">{{ $song->category?->name ?: 'Sin categoría' }} · {{ $song->liturgicalMoment?->name ?: 'Sin momento' }}</div></div><div class="card-footer border-0 app-page-actions"><a class="btn btn-sm btn-primary" href="{{ route('songs.show',$song) }}"><i class="bi bi-eye"></i>Ver detalle</a>@can('update', $song)<a class="btn btn-sm btn-outline-secondary" href="{{ route('songs.edit',$song) }}"><i class="bi bi-pencil"></i>Editar</a>@endcan</div></article></div>@empty<div class="col-12"><div class="card app-empty-state"><i class="bi bi-music-note-list"></i><h2 class="h4">{{ $scope === 'mine' ? 'Todavía no tienes canciones' : 'No se encontraron canciones' }}</h2><p>{{ $scope === 'mine' ? 'Sube tu primera canción para comenzar.' : 'Prueba con otros términos o filtros.' }}</p>@if($scope === 'mine')<a class="btn btn-primary" href="{{ route('songs.create') }}"><i class="bi bi-plus-lg"></i>Nueva canción</a>@endif</div></div>@endforelse</div><div class="app-list-footer">{{ $songs->onEachSide(1)->links() }}</div>
@endsection