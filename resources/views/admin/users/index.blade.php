@extends('layouts.app')
@section('title','Usuarios')
@section('content')
<div class="app-page-header"><div><h1>Usuarios</h1><p>Administra el acceso de las cuentas registradas.</p></div></div>
<form class="card card-body app-filter-card" method="GET" role="search"><div class="row g-2"><div class="col-md-9"><label class="visually-hidden" for="user-q">Buscar por nombre o correo</label><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" id="user-q" name="q" value="{{ request('q') }}" placeholder="Nombre o correo"></div></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-primary flex-grow-1"><i class="bi bi-search"></i>Buscar</button><a class="btn btn-outline-secondary" href="{{ route('admin.users.index') }}">Limpiar</a></div></div></form>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Usuario</th><th>Rol</th><th>Contenido</th><th>Verificación</th><th>Estado</th><th class="text-end">Acción</th></tr></thead><tbody>@forelse($users as $user)<tr><td><strong>{{ $user->name }}</strong><small class="d-block text-secondary">{{ $user->email }}</small></td><td><span class="badge {{ $user->is_admin ? 'text-bg-primary' : 'text-bg-light' }}">{{ $user->is_admin ? 'Administrador' : 'Usuario' }}</span></td><td><span class="text-nowrap">{{ $user->songs_count }} canciones</span><small class="d-block text-secondary">{{ $user->repertoires_count }} repertorios</small></td><td>{{ $user->hasVerifiedEmail() ? 'Verificado' : 'Pendiente' }}</td><td><span class="badge {{ $user->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $user->is_active ? 'Activa' : 'Desactivada' }}</span></td><td class="text-end"><form method="POST" action="{{ route('admin.users.update',$user) }}">@csrf @method('PUT')<input type="hidden" name="is_active" value="{{ $user->is_active ? 0 : 1 }}"><button class="btn btn-sm {{ $user->is_active ? 'btn-outline-danger' : 'btn-outline-primary' }}" @disabled(auth()->user()->is($user))>{{ $user->is_active ? 'Desactivar' : 'Activar' }}</button></form></td></tr>@empty<tr><td colspan="6"><div class="app-empty-state"><i class="bi bi-people"></i>No se encontraron usuarios.</div></td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $users->links() }}</div>
@endsection