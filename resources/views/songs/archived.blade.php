@extends('layouts.app')
@section('title','Canciones archivadas')
@section('content')
<div class="app-page-header"><div><a class="app-back-link" href="{{ route('songs.index',['scope'=>'mine']) }}"><i class="bi bi-arrow-left"></i> Mis canciones</a><h1 class="mt-2">Canciones archivadas</h1><p>Restaura canciones sin perder sus archivos ni relaciones.</p></div></div>
<form class="card card-body app-filter-card" method="GET" role="search"><div class="row g-2"><div class="col-md-7"><label class="visually-hidden" for="archived-q">Buscar canción archivada</label><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" id="archived-q" name="q" value="{{ request('q') }}" placeholder="Título o autor"></div></div><div class="col-sm-5 col-md-2"><label class="visually-hidden" for="archived-per-page">Resultados por página</label><select class="form-select" id="archived-per-page" name="per_page" onchange="this.form.submit()"><option value="12" @selected($songs->perPage() === 12)>12 por página</option><option value="24" @selected($songs->perPage() === 24)>24 por página</option><option value="48" @selected($songs->perPage() === 48)>48 por página</option></select></div><div class="col-sm-7 col-md-3 d-flex gap-2"><button class="btn btn-primary flex-grow-1"><i class="bi bi-search"></i>Buscar</button><a class="btn btn-outline-secondary" href="{{ route('songs.archived') }}">Limpiar</a></div></div></form>
<div class="app-list-toolbar"><p class="app-result-count">@if($songs->total()) Mostrando <strong>{{ $songs->firstItem() }}–{{ $songs->lastItem() }}</strong> de <strong>{{ $songs->total() }}</strong> canciones @else Sin resultados @endif</p>@if($songs->hasPages())<div class="app-list-pagination">{{ $songs->onEachSide(1)->links() }}</div>@endif</div>
<div class="card app-list-card"><div class="list-group list-group-flush">@forelse($songs as $song)<div class="list-group-item app-management-row"><div class="app-row-icon"><i class="bi bi-music-note"></i></div><div class="flex-grow-1"><strong>{{ $song->title }}</strong><div class="small text-secondary">{{ $song->author ?: 'Autor no indicado' }} · Subida por {{ $song->owner->name }}</div><div class="small text-secondary">Archivada el {{ $song->deleted_at->format('d-m-Y H:i') }}</div></div><div class="d-flex flex-wrap gap-2"><form method="POST" action="{{ route('songs.restore',$song->id) }}">@csrf @method('PUT')<button class="btn btn-outline-primary"><i class="bi bi-arrow-counterclockwise"></i>Restaurar</button></form><form method="POST" action="{{ route('songs.force-destroy',$song->id) }}" data-confirm="¿Eliminar definitivamente esta canción y todos sus archivos? Esta acción no se puede deshacer." data-confirm-text="ELIMINAR CANCION" data-confirm-prompt="Escribe ELIMINAR CANCION para borrar definitivamente la canción y sus archivos.">@csrf @method('DELETE')<button class="btn btn-outline-danger"><i class="bi bi-trash3"></i>Eliminar definitivamente</button></form></div></div>@empty<div class="app-empty-state"><i class="bi bi-archive"></i><h2 class="h4">No hay canciones archivadas</h2><p class="mb-0">Las canciones que archives aparecerán aquí.</p></div>@endforelse</div></div><div class="app-list-footer">{{ $songs->onEachSide(1)->links() }}</div>
@endsection